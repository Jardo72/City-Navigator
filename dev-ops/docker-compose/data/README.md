# Data

This directory is a Docker Compose bind mount target for the SQLite database used by the Master Data Service.
The database file itself is gitignored; this file exists solely to preserve the directory in version control after a fresh clone.
